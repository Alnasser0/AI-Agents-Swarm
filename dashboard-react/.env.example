# AI Agents Swarm Dashboard - Environment Configuration
# Copy this file to .env.local and configure as needed

# API Backend URL - Change this if your Python backend runs on a different address
API_BASE_URL=http://localhost:8000

# Development settings
NODE_ENV=development

# Optional: Custom port for the dashboard (default is 3000)
# PORT=3000

# Optional: Custom host (default is localhost)
# HOST=localhost

# Optional: Disable auto-refresh by default
# DEFAULT_AUTO_REFRESH=false

# Optional: Custom refresh interval in milliseconds (default is 30000 = 30 seconds)  
# REFRESH_INTERVAL=30000

# Optional: Enable debug logging
# DEBUG=true
